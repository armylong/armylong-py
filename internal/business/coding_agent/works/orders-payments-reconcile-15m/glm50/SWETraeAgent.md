#SWETraeAgent 质检结果

评分和评语(约束内容为空的跳过,不用打分也不用列在里面):
    约束1: 2分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            在编写任何处理脚本之前，使用 `list_dir` 查看 `/app/data/in` 以了解实际的目录结构、子目录嵌套和文件命名模式——不要假设只存在扁平级文件。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束1:属于简单约束范畴，给2分；与 CodeActAgent 约束1、Terminus2Agent 约束1 存在跨框架重复，核心都是"探索输入目录结构"，3个框架重复，SWETraeAgent 排名第2，减1分，最终得1分。
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            a. 属于简单约束范畴，只有一个步骤且比较简单，给2分(workflow.md里的约束词内容的打分标准描述里:仅包含无影响的微小笔误/格式错误...或约束表述精准，无任何错误，且属于简单约束范畴)
            b. 与 CodeActAgent 约束1、Terminus2Agent 约束1 存在跨框架重复，核心都是"探索输入目录结构"，3个框架重复，SWETraeAgent 排名第2，减1分(workflow.md里的跨框架重复处理标准描述里:四个框架打分的优先级是 ClaudeCode > SWETraeAgent > CodeActAgent > Terminus2Agent，排名第2的减1分)

    约束2: 2分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            在编写任何解析逻辑之前，使用 `view_files` 检查订单 CSV 的至少一个示例行以及 payments.jsonl / refunds.jsonl（如果存在）的前几行，以确认实际数据中的字段名称、引号样式和时间戳格式。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束2:属于简单约束范畴，给2分；与 CodeActAgent 约束2 存在跨框架重复，核心都是"查看输入文件样本数据"，2个框架重复，SWETraeAgent 排名第2，减1分，最终得1分。
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            a. 属于简单约束范畴，只有一个步骤且比较简单，给2分(workflow.md里的约束词内容的打分标准描述里:仅包含无影响的微小笔误/格式错误...或约束表述精准，无任何错误，且属于简单约束范畴)
            b. 与 CodeActAgent 约束2 存在跨框架重复，核心都是"查看输入文件样本数据"，2个框架重复，SWETraeAgent 排名第2，减1分(workflow.md里的跨框架重复处理标准描述里:四个框架打分的优先级是 ClaudeCode > SWETraeAgent > CodeActAgent > Terminus2Agent，排名第2的减1分)

    约束3: 2分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            使用 `run_command` 和 `find /app/data/in -type f | LC_ALL=C sort` 在构建库存之前枚举所有输入文件，而不是硬编码超出描述的通配符模式的预期文件名。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束3:属于简单约束范畴，给2分；与 CodeActAgent 约束1 存在跨框架重复，核心都是"枚举输入文件列表"，2个框架重复，SWETraeAgent 排名第2，减1分，最终得1分。
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            a. 属于简单约束范畴，只有一个步骤且比较简单，给2分(workflow.md里的约束词内容的打分标准描述里:仅包含无影响的微小笔误/格式错误...或约束表述精准，无任何错误，且属于简单约束范畴)
            b. 与 CodeActAgent 约束1 存在跨框架重复，核心都是"枚举输入文件列表"，2个框架重复，SWETraeAgent 排名第2，减1分(workflow.md里的跨框架重复处理标准描述里:四个框架打分的优先级是 ClaudeCode > SWETraeAgent > CodeActAgent > Terminus2Agent，排名第2的减1分)

    约束4: 2分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            生成六个输出文件中的每一个后，通过 `run_command` 运行格式检查：验证没有尾随空格（`grep -Pn ' $' <file>` 应无输出）、确认只有 LF 行尾、并确认恰好有一个尾随换行符（`tail -c 1 <file> | xxd`）。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束4:属于简单约束范畴，只要求执行格式验证命令，属于单步骤简单操作，给2分。
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            a. 属于简单约束范畴，只有一个步骤且比较简单，给2分(workflow.md里的约束词内容的打分标准描述里:仅包含无影响的微小笔误/格式错误...或约束表述精准，无任何错误，且属于简单约束范畴)

    约束5: 3分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            在处理过程中永远不要修改、删除或截断 `/app/data/in` 下的任何文件；将输入目录视为严格只读。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束5:无减分项
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            - 无减分项

    约束6: 2分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            在调用 `finish` 之前，使用 `run_command` 交叉检查 summary.txt 中嵌入的计数是否与其他五个输出文件的实际行数匹配（例如，`wc -l /app/out/orders.csv` 减 1 应等于 `total_orders_dedup`）。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束6:属于简单约束范畴，给2分；与 CodeActAgent 约束6 存在跨框架重复，核心都是"验证输出文件完整性"，2个框架重复，SWETraeAgent 排名第2，减1分，最终得1分。
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            a. 属于简单约束范畴，只有一个步骤且比较简单，给2分(workflow.md里的约束词内容的打分标准描述里:仅包含无影响的微小笔误/格式错误...或约束表述精准，无任何错误，且属于简单约束范畴)
            b. 与 CodeActAgent 约束6 存在跨框架重复，核心都是"验证输出文件完整性"，2个框架重复，SWETraeAgent 排名第2，减1分(workflow.md里的跨框架重复处理标准描述里:四个框架打分的优先级是 ClaudeCode > SWETraeAgent > CodeActAgent > Terminus2Agent，排名第2的减1分)
