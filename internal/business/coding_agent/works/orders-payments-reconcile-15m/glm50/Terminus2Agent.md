#Terminus2Agent 质检结果

评分和评语(约束内容为空的跳过,不用打分也不用列在里面):
    约束1: 1分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            在编写任何对账脚本之前，在终端中运行 `ls -la /app/data/in/`，然后对至少一个订单 CSV、一个 payments JSONL 和一个 refunds JSONL（如果存在）运行 `head -5`，以在假设任何数据之前观察真实的字段名称、引号样式和时间戳格式。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束1:属于简单约束范畴，给2分；与 SWETraeAgent 约束1、CodeActAgent 约束1 存在跨框架重复，核心都是"探索输入目录结构"，3个框架重复，Terminus2Agent 排名第4，减3分，最低1分，最终得1分。
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            a. 属于简单约束范畴，只有一个步骤且比较简单，给2分(workflow.md里的约束词内容的打分标准描述里:仅包含无影响的微小笔误/格式错误...或约束表述精准，无任何错误，且属于简单约束范畴)
            b. 与 SWETraeAgent 约束1、CodeActAgent 约束1 存在跨框架重复，核心都是"探索输入目录结构"，3个框架重复，Terminus2Agent 排名第4，减3分，最低1分(workflow.md里的跨框架重复处理标准描述里:四个框架打分的优先级是 ClaudeCode > SWETraeAgent > CodeActAgent > Terminus2Agent，排名第4的减3分，最低只能到1分)

    约束2: 1分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            在执行解决方案脚本之前检查 `/app/out` 是否存在；如果不存在，使用 `mkdir -p /app/out` 创建它，以便后续写入操作不会静默失败。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束2:属于简单约束范畴，给2分；与 ClaudeCode 约束3 存在跨框架重复，核心都是"确保输出目录存在"，2个框架重复，Terminus2Agent 排名第4，减3分，最低1分，最终得1分。
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            a. 属于简单约束范畴，只有一个步骤且比较简单，给2分(workflow.md里的约束词内容的打分标准描述里:仅包含无影响的微小笔误/格式错误...或约束表述精准，无任何错误，且属于简单约束范畴)
            b. 与 ClaudeCode 约束3 存在跨框架重复，核心都是"确保输出目录存在"，2个框架重复，Terminus2Agent 排名第4，减3分，最低1分(workflow.md里的跨框架重复处理标准描述里:四个框架打分的优先级是 ClaudeCode > SWETraeAgent > CodeActAgent > Terminus2Agent，排名第4的减3分，最低只能到1分)

    约束3: 2分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            脚本完成后，在终端中运行 `head -3 <file>` 和 `wc -l <file>` 验证六个输出文件中的每一个。在设置 task_complete 之前确认标题存在且行数为非负数。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束3:属于简单约束范畴，只要求执行验证命令检查输出文件，属于单步骤简单操作，给2分。
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            a. 属于简单约束范畴，只有一个步骤且比较简单，给2分(workflow.md里的约束词内容的打分标准描述里:仅包含无影响的微小笔误/格式错误...或约束表述精准，无任何错误，且属于简单约束范畴)
