#ClaudeCode 质检结果

评分和评语(约束内容为空的跳过,不用打分也不用列在里面):
    约束1: 3分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            使用 `jq` 解析 JSONL 文件（payments.jsonl、refunds.jsonl）。不要尝试用 awk/sed/grep 正则表达式解析 JSON——这在处理转义字符和嵌套结构时容易出错。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束1:无减分项
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            - 无减分项

    约束2: 3分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            构建中间临时文件时，将它们放在 /tmp/reconcile/ 下（使用 `mkdir -p` 创建）。不要用中间产物污染 /app/data/in 或 /app/out。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束2:无减分项
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            - 无减分项

    约束3: 2分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            在主处理之前，使用 Bash 确认 `/app/out` 存在：`mkdir -p /app/out`。然后在脚本执行后使用 `ls /app/out/` 验证所有六个文件都已创建。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束3:属于简单约束范畴，给2分；与 Terminus2Agent 约束2 存在跨框架重复，核心都是"确保输出目录存在"，2个框架重复，ClaudeCode 排名第1，不减分，最终得2分。
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            a. 属于简单约束范畴，只有一个步骤且比较简单，给2分(workflow.md里的约束词内容的打分标准描述里:仅包含无影响的微小笔误/格式错误...或约束表述精准，无任何错误，且属于简单约束范畴)

    约束4: 3分
        1. 子约束内容(翻译成中文, 不需要展示原始英文版)
            使用 TodoWrite 概述处理阶段（库存扫描 → 订单去重 → 支付匹配 → 退款匹配 → 会话构建 → 摘要），在编写脚本之前确保没有遗漏任何输出文件。
        2. 输出(严格按照下面格式, 因为我得复制使用, 格式是: 约束[约束id]:***)
            约束4:无减分项
        3. 分数计算过程和依据(每一项都要列出来依赖条款)
            - 无减分项
